Remover cosas innecesarias en el backend (Mutex, Locks, etc...)
Hacer settings
Quitar consola
poner banner

Sobre el backend:
hacer refactor dentro

Cosas a hacer:
x - Que las instances se manejen con un actor que sincroniza el estado en la memoria ram con el fs
x - Instances mejor estructuradas

pub struct InstanceData {
    name: String,
    version: String,
    last_played: u64,
    min_memory: Option<u32>,
    max_memory: Option<u32>,
    cover_image: Option<PathBuf>,
    // Eliminamos process y dirty flags - el proceso se maneja aparte
}

pub struct Instance {
    data: InstanceData,
    process: Option<tokio::process::Child>, // Usamos Child de tokio
}

remover doble locks Lazy y mutex
remover arcs
x - sincronizacion cada 30 segundos

POSIBLE YIELD EN COMANDOS
